import 'package:flutter/material.dart';

class RecipieBookScreen extends StatelessWidget {
  const RecipieBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Recipe Collections")));
  }
}
